[project]
name = "credit_risk_assessment"
version = "0.1.0"
description = "Modularized credit risk assessment pipeline"
dependencies = [
    "numpy",
    "pandas",
    "scikit-learn",
    "matplotlib",
    "seaborn",
    "pytest",
]

[tool.pytest.ini_options]
pythonpath = ["."]
